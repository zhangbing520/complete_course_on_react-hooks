interface SyncOptions {
    /** Filename of meta */
    metaFileName?: string;
    /** Filename of Arco CLI config */
    configFileName?: string;
    /** Whether to automatically screenshot */
    screenshot?: boolean;
    /** Paths to exec this command */
    paths?: string | string[];
    /** Whether to update the local data from origin */
    fetch?: boolean;
}
/**
 * Synchronize material meta
 */
export default function ({ metaFileName, configFileName, screenshot, paths, fetch, }?: SyncOptions): Promise<void>;
export {};
