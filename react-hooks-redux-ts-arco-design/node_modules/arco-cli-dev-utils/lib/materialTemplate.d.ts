interface TransformToProjectOptions {
    root: string;
    packageJson: {
        [key: string]: any;
    };
}
interface TransformToTemplateOptions {
    root: string;
}
export declare const transformToProject: ({ root, packageJson: packageJsonToExtend, }: TransformToProjectOptions) => Promise<void>;
export declare const transformToTemplate: ({ root }: TransformToTemplateOptions) => Promise<void>;
export {};
