export default function execQuick(command: string, options?: {
    cwd?: string;
    time?: boolean;
    silent?: boolean;
}): Promise<{
    pid: number;
    code: number;
    stdout: string;
    stderr: string;
}>;
