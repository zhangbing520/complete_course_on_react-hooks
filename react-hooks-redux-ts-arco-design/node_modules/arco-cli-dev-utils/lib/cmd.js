"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.execQuick = void 0;
const child_process_1 = require("child_process");
const print_1 = __importDefault(require("./print"));
function execQuick(command, cwd = process.cwd(), options = {
    time: false,
    display: false,
}) {
    return __awaiter(this, void 0, void 0, function* () {
        return new Promise((resolve, reject) => {
            const begin = new Date().getTime();
            const stdout = [];
            const task = (0, child_process_1.spawn)(command, [], {
                shell: true,
                cwd,
            });
            task.stdout.on('end', () => {
                const end = new Date().getTime();
                const waste = ((end - begin) / 1000).toFixed(2);
                if (options.time) {
                    print_1.default.info(command, `Executed in ${waste} ms.`);
                }
                resolve({
                    pid: task.pid,
                    stdout,
                });
            });
            task.stdout.on('data', (data) => {
                const dataStr = data.toString('utf-8');
                if (options.display) {
                    (0, print_1.default)(dataStr);
                }
                stdout.push(dataStr);
            });
            task.stdout.on('error', (err) => {
                print_1.default.error(command, 'Executed failed');
                console.error(err);
                reject(err);
            });
        });
    });
}
exports.execQuick = execQuick;
