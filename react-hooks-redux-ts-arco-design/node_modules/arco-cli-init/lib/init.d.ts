interface ProjectInitOptions {
    projectName: string;
    template?: string;
    metaFileName?: string;
    configFileName?: string;
    isForMonorepo?: boolean;
    isPureProject?: boolean;
}
/**
 * Init project
 */
export default function ({ projectName, template, metaFileName, configFileName, isForMonorepo, isPureProject, }: ProjectInitOptions): Promise<void>;
export {};
