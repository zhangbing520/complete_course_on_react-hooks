interface LogParameters {
    user: {
        username: string;
    };
    event: {
        event: string;
        time: number;
        params: Record<string, any>;
    };
}
export default function teaLog(params: LogParameters['event']['params']): void;
export {};
