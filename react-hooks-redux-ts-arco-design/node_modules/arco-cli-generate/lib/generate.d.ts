export interface GenerateOptions {
    metaFileName?: string;
    configFileName?: string;
    paths?: string | string[];
    meta?: {
        [key: string]: any;
    };
    silent?: boolean;
}
declare const _default: ({ metaFileName, configFileName, paths, meta: initialMeta, silent, }?: GenerateOptions) => Promise<boolean>;
/**
 * Generate material meta info in project
 */
export default _default;
